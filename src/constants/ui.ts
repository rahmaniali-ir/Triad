export const NOTE_ELEMENT_SIZE = 32
export const NUM_COPIES = 15
export const ARROW_HEIGHT = 20
/** Number of scale-widths to keep as buffer from each edge before jumping (infinite scroll). */
export const INFINITE_SCROLL_BUFFER = 4
